import { makeStyles } from "tss-react/mui";

export const useStyles = makeStyles<Partial<any>>()(
  (theme, {  }) => ({
    frame: {

    },
    featured: {

    },
    predominent: {
        
    }
  })
);
